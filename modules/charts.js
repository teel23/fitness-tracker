const chartInstances = new Map(); export const COLORS = { accent: '#5eb8f0', accentDim: 'rgba(94, 184, 240, 0.1)', accentMid: 'rgba(94, 184, 240, 0.7)', secondary: '#a78bfa', secondaryDim: 'rgba(167, 139, 250, 0.08)', secondaryMid: 'rgba(167, 139, 250, 0.7)', tertiary: '#f472b6', tertiaryDim: 'rgba(244, 114, 182, 0.1)', tertiaryMid: 'rgba(244, 114, 182, 0.7)', text: '#e8e8e8', textSecondary: '#888888', surface: '#1a1a1a', surfaceElevated: '#252525', border: '#2a2a2a', gridLine: 'rgba(42, 42, 42, 0.5)', }; export function getBaseChartOptions() { return { responsive: true, maintainAspectRatio: true, animation: { duration: 300 }, plugins: { legend: { display: true, labels: { color: COLORS.text, font: { family: "-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif", size: 12 }, }, }, tooltip: { backgroundColor: COLORS.surfaceElevated, titleColor: COLORS.text, bodyColor: COLORS.textSecondary, borderColor: COLORS.border, borderWidth: 1, cornerRadius: 8, padding: 10, }, }, scales: { x: { type: 'time', time: { tooltipFormat: 'MMM d, yyyy' }, ticks: { color: COLORS.textSecondary, font: { size: 11 }, maxTicksLimit: 8 }, grid: { color: COLORS.gridLine, drawBorder: false }, }, y: { beginAtZero: false, ticks: { color: COLORS.textSecondary, font: { size: 11 } }, grid: { color: COLORS.gridLine, drawBorder: false }, }, }, }; } export function createOrUpdateChart(canvasId, config) { destroyChart(canvasId); const canvas = document.getElementById(canvasId); if (!canvas) return null; const ctx = canvas.getContext('2d'); const chart = new Chart(ctx, config); chartInstances.set(canvasId, chart); return chart; } export function destroyChart(canvasId) { if (chartInstances.has(canvasId)) { chartInstances.get(canvasId).destroy(); chartInstances.delete(canvasId); } } export function destroyAllCharts() { for (const [id, chart] of chartInstances) { chart.destroy(); } chartInstances.clear(); } function getTimeUnit(period) { switch (period) { case '1M': return 'day'; case '3M': return 'week'; case '6M': return 'week'; case '1Y': return 'month'; case 'all': return 'month'; default: return 'week'; } } export function buildStrength1RMConfig(data, period) { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); return { type: 'line', data: { datasets: [{ label: 'Estimated 1RM', data, borderColor: COLORS.accent, backgroundColor: COLORS.accentDim, fill: true, tension: 0.3, pointRadius: 4, pointBackgroundColor: COLORS.accent, pointBorderColor: COLORS.surface, pointBorderWidth: 2, }], }, options: { ...base, scales: { ...base.scales, y: { ...base.scales.y, title: { display: true, text: '1RM', color: COLORS.textSecondary } }, }, }, }; } export function buildStrengthVolumeConfig(data, period, yLabel = 'Volume') { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); return { type: 'bar', data: { datasets: [{ label: yLabel, data, backgroundColor: COLORS.secondaryMid, borderColor: COLORS.secondary, borderWidth: 1, borderRadius: 4, }], }, options: { ...base, scales: { ...base.scales, y: { ...base.scales.y, beginAtZero: true, title: { display: true, text: yLabel, color: COLORS.textSecondary } }, }, }, }; } export function buildCardioDistanceConfig(data, unit, period) { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); return { type: 'line', data: { datasets: [{ label: `Distance (${unit})`, data, borderColor: COLORS.tertiary, backgroundColor: COLORS.tertiaryDim, fill: true, tension: 0.3, pointRadius: 4, pointBackgroundColor: COLORS.tertiary, pointBorderColor: COLORS.surface, pointBorderWidth: 2, }], }, options: { ...base, scales: { ...base.scales, y: { ...base.scales.y, title: { display: true, text: `Distance (${unit})`, color: COLORS.textSecondary } }, }, }, }; } export function buildCardioPaceConfig(data, unit, period) { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); return { type: 'line', data: { datasets: [{ label: `Pace (min/${unit})`, data, borderColor: COLORS.tertiary, backgroundColor: 'rgba(244, 114, 182, 0.08)', fill: true, tension: 0.3, pointRadius: 4, pointBackgroundColor: COLORS.tertiary, }], }, options: { ...base, scales: { ...base.scales, y: { ...base.scales.y, reverse: true, title: { display: true, text: `min/${unit} (lower is faster)`, color: COLORS.textSecondary }, }, }, }, }; } export function buildBodyWeightConfig(weightData, bfData, weightUnit, period) { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); const datasets = [{ label: `Weight (${weightUnit})`, data: weightData, borderColor: COLORS.accent, backgroundColor: COLORS.accentDim, fill: true, tension: 0.3, pointRadius: 3, pointBackgroundColor: COLORS.accent, yAxisID: 'y', }]; const scales = { x: base.scales.x, y: { ...base.scales.y, position: 'left', title: { display: true, text: `Weight (${weightUnit})`, color: COLORS.textSecondary }, }, }; if (bfData && bfData.length > 0) { datasets.push({ label: 'Body Fat %', data: bfData, borderColor: COLORS.secondary, backgroundColor: COLORS.secondaryDim, fill: true, tension: 0.3, pointRadius: 3, pointBackgroundColor: COLORS.secondary, yAxisID: 'y1', }); scales.y1 = { ...base.scales.y, position: 'right', title: { display: true, text: 'Body Fat %', color: COLORS.textSecondary }, grid: { drawOnChartArea: false }, }; } return { type: 'line', data: { datasets }, options: { ...base, scales }, }; } export function buildBodyMetricLineConfig(data, label, color, colorDim, period, yAxisLabel) { const base = getBaseChartOptions(); base.scales.x.time.unit = getTimeUnit(period); return { type: 'line', data: { datasets: [{ label, data, borderColor: color, backgroundColor: colorDim, fill: true, tension: 0.3, pointRadius: 3, pointBackgroundColor: color, }], }, options: { ...base, scales: { ...base.scales, y: { ...base.scales.y, title: { display: true, text: yAxisLabel || label, color: COLORS.textSecondary } }, }, }, }; } export function buildMiniLineConfig(data, label, color, colorDim) { return { type: 'line', data: { datasets: [{ label, data, borderColor: color, backgroundColor: colorDim, fill: true, tension: 0.3, pointRadius: 2, pointBackgroundColor: color, borderWidth: 2, }], }, options: { responsive: true, maintainAspectRatio: true, animation: { duration: 200 }, plugins: { legend: { display: false }, tooltip: { backgroundColor: COLORS.surfaceElevated, titleColor: COLORS.text, bodyColor: COLORS.textSecondary, borderColor: COLORS.border, borderWidth: 1, cornerRadius: 6, padding: 8, }, }, scales: { x: { type: 'time', time: { unit: 'day', tooltipFormat: 'MMM d' }, display: false, }, y: { beginAtZero: false, ticks: { color: COLORS.textSecondary, font: { size: 10 }, maxTicksLimit: 4 }, grid: { color: COLORS.gridLine, drawBorder: false }, }, }, }, }; } export function buildMiniBarConfig(data, label, color) { return { type: 'bar', data: { datasets: [{ label, data, backgroundColor: color, borderRadius: 3, barPercentage: 0.6, }], }, options: { responsive: true, maintainAspectRatio: true, animation: { duration: 200 }, plugins: { legend: { display: false }, tooltip: { backgroundColor: COLORS.surfaceElevated, titleColor: COLORS.text, bodyColor: COLORS.textSecondary, borderColor: COLORS.border, borderWidth: 1, cornerRadius: 6, padding: 8, }, }, scales: { x: { type: 'time', time: { unit: 'day', tooltipFormat: 'MMM d' }, ticks: { color: COLORS.textSecondary, font: { size: 10 }, maxTicksLimit: 7 }, grid: { display: false }, }, y: { beginAtZero: true, ticks: { color: COLORS.textSecondary, font: { size: 10 }, maxTicksLimit: 4 }, grid: { color: COLORS.gridLine, drawBorder: false }, }, }, }, }; }
