export const MUSCLE_COLORS = { chest: '#ef4444', back: '#3b82f6', shoulders: '#a78bfa', arms: '#10b981', legs: '#9ca3af', core: '#f472b6', cardio: '#06b6d4', }; export function createProgressRing(percentage, opts = {}) { const { size = 80, strokeWidth = 7, color = '#5eb8f0', label = '', value = '', } = opts; const radius = (size - strokeWidth) / 2; const circumference = 2 * Math.PI * radius; const offset = circumference - (Math.min(percentage, 100) / 100) * circumference; const center = size / 2; return ` <div class="progress-ring" style="width:${size}px"> <svg width="${size}" height="${size}" viewBox="0 0 ${size} ${size}"> <circle cx="${center}" cy="${center}" r="${radius}" fill="none" stroke="var(--border)" stroke-width="${strokeWidth}" /> <circle cx="${center}" cy="${center}" r="${radius}" fill="none" stroke="${color}" stroke-width="${strokeWidth}" stroke-dasharray="${circumference}" stroke-dashoffset="${offset}" stroke-linecap="round" transform="rotate(-90 ${center} ${center})" style="transition: stroke-dashoffset 0.6s ease" /> </svg> <div class="progress-ring__info"> <span class="progress-ring__value">${value}</span> <span class="progress-ring__label">${label}</span> </div> </div>`; } export function calculateFrequency(dates, period) { const now = new Date(); let startDate; let target; if (period === 'week') { const day = now.getDay(); const diff = day === 0 ? 6 : day - 1; startDate = new Date(now); startDate.setDate(now.getDate() - diff); startDate.setHours(0, 0, 0, 0); target = 5; } else { startDate = new Date(now.getFullYear(), now.getMonth(), 1); target = 20; } const count = dates.filter(d => new Date(d) >= startDate).length; return { count, target, percentage: Math.min(100, (count / target) * 100) }; } export function getWorkoutColor(workout, exercises) { if (!workout.exercises || !workout.exercises.length) return MUSCLE_COLORS.chest; const exId = workout.exercises[0].exerciseId; const ex = exercises.find(e => e.id === exId); return ex ? (MUSCLE_COLORS[ex.category] || '#5eb8f0') : '#5eb8f0'; }
