let confirmCallback = null; export function showConfirmDialog(message, callback) { document.getElementById('confirm-message').textContent = message; confirmCallback = callback; document.getElementById('confirm-dialog').hidden = false; } export function closeConfirmDialog() { document.getElementById('confirm-dialog').hidden = true; confirmCallback = null; } export function confirmOk() { if (confirmCallback) confirmCallback(); closeConfirmDialog(); }
