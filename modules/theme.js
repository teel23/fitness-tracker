const STORAGE_KEY = 'fittrack-theme'; let currentTheme = 'dark'; export function init() { const saved = localStorage.getItem(STORAGE_KEY); if (saved) { currentTheme = saved; } else { currentTheme = window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark'; } applyTheme(); } export function toggle() { currentTheme = currentTheme === 'dark' ? 'light' : 'dark'; document.documentElement.classList.add('theme-transitioning'); applyTheme(); localStorage.setItem(STORAGE_KEY, currentTheme); setTimeout(() => document.documentElement.classList.remove('theme-transitioning'), 350); } export function setTheme(theme) { if (theme !== 'dark' && theme !== 'light') return; currentTheme = theme; document.documentElement.classList.add('theme-transitioning'); applyTheme(); localStorage.setItem(STORAGE_KEY, currentTheme); setTimeout(() => document.documentElement.classList.remove('theme-transitioning'), 350); } export function getCurrentTheme() { return currentTheme; } function applyTheme() { document.documentElement.setAttribute('data-theme', currentTheme); }
